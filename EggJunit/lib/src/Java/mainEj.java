package lib.src.Java;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.io.ByteArrayInputStream;
import java.util.Scanner;

public class mainEj
{
    public static void main( String[] args )
    {
        System.out.println( "Hello World!" );
    }

    public void ejemplo(){
        Scanner numero = new Scanner(System.in);
        // System.out.println(numero);;
        // ByteArrayInputStream in = new ByteArrayInputStream("10\n".getBytes()); System.setIn(in);
        // Ejercicio4.main(new String[1]);
        // String salida = outputStreamCaptor.toString();
        // String[] salidas = salida.split(regex:"\r\n");
        // System.setOut (standardOut);
        // assertEquals("El doble de "+numero+" es 30", salidas[0]);
        // assertEquals("El triple de "+numero+" es 45", salidas[1]);
    }
}